# 🎯 Quiz Bot (Telegram + VK)

Бот-викторина для Telegram и ВКонтакте.\
Пользователь получает случайный вопрос, может ответить на него или
сдаться.\
Вопросы и состояние пользователя хранятся в Redis.

------------------------------------------------------------------------

## 🚀 Возможности

-   📩 Новый случайный вопрос\
-   ✅ Проверка ответа\
-   ❌ Возможность сдаться (показывает ответ и задаёт новый вопрос)\
-   💾 Хранение состояния пользователя в Redis\
-   🤖 Поддержка Telegram и VK\
-   ⚠️ Уведомление администратора при ошибках

------------------------------------------------------------------------

## 🗂 Структура проекта

    .
    ├── tg_bot.py
    ├── vk_bot.py
    ├── functions.py
    ├── error_handler.py
    ├── questions/
    │   └── questions.txt
    ├── requirements.txt
    └── README.md

------------------------------------------------------------------------

## ⚙️ Установка

### 1️⃣ Клонировать репозиторий

``` bash
git clone https://github.com/manilotw/QuizBots.git
cd quiz-bot
```

### 2️⃣ Создать виртуальное окружение

``` bash
python -m venv venv
source venv/bin/activate   # Linux / Mac
venv\Scripts\activate      # Windows
```

### 3️⃣ Установить зависимости

``` bash
pip install -r requirements.txt
```

------------------------------------------------------------------------

## 🔑 Настройка переменных окружения

Создайте файл `.env` в корне проекта:

    TELEGRAM_BOT_TOKEN=your_telegram_token
    TELEGRAM_CHAT_ID=admin_chat_id

    VK_API_KEY=your_vk_group_token

    REDIS_URL=host
    REDIS_PORT=port
    REDIS_PASSWORD=password

------------------------------------------------------------------------

## 🧠 Redis

Проект использует Redis для хранения:

-   текущего вопроса пользователя
-   состояния диалога

Можно использовать:

-   Redis Cloud
-   локальный Redis
-   Docker

------------------------------------------------------------------------

## ▶️ Запуск

### Telegram-бот

``` bash
python tg_bot.py
```

### VK-бот

``` bash
python vk_bot.py
```

------------------------------------------------------------------------

## 📚 Формат вопросов

Файл `questions.txt` должен содержать блоки вида:

    Вопрос:
    Текст вопроса

    Ответ:
    Правильный ответ.

Пояснения после точки и в скобках автоматически игнорируются при
проверке.

------------------------------------------------------------------------

## 🛡 Обработка ошибок

При возникновении ошибки бот:

-   отправляет уведомление администратору
-   логирует исключение

------------------------------------------------------------------------

## 🧩 Используемые технологии

-   python-telegram-bot
-   vk-api
-   redis
-   environs

------------------------------------------------------------------------

## Пример использования бота

[Телеграм-бот](https://t.me/viktorinaaasbot)
[ВК-бот](https://vk.com/im?sel=-235957838) 